import React, { FunctionComponent, useState } from "react";
import {useParams} from "react-router";

export const Home: React.FunctionComponent<{}> = (props) => {
    return <>
        <h1>lmao choose a room</h1>
    </>
}